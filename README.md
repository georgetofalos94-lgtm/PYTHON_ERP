ERP in python
This project uses "sqlite3", that creates a database for our project
the code has pre-existing log in credentials: 
username "useradmin01" 
password "usradmn0100"
That lets you log in as an administrator with elevated privileges (
As an administrator you create users and you can choose whether they have administrator rights or restricted permissions
